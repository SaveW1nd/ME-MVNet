model:
  n_samples: 4000
  nf_values: [1, 2, 4]

  raw_branch:
    stem_channels: [64, 128]
    transformer_layers: 1
    transformer_heads: 4
    dropout: 0.1

  tf_branch:
    n_fft: 256
    hop_length: 64
    win_length: 256
    channels: [32, 64, 128]

  mech_branch:
    feature_dim: 32
    hidden_dim: 64

  fusion:
    hidden_dim: 128

  mask_head:
    hidden_dim: 64
