# Runs

Each experiment directory (`exp_xxx`) stores:

- `config_dump.yaml`
- `checkpoints/{best.pt,last.pt}`
- `logs/{train.log,metrics.jsonl}`
- `predictions/{val_pred.npz,test_pred.npz}`
- `figures/*.png`
- `tables/*.csv|json`
